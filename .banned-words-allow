discovery
unlock

# cadence. Newman's Building Microservices and DORA release-cadence
# terminology, and the repo's own prior justification for this exact word.
cadence

# at scale. Verbatim inside a direct quote from Kubernetes' own docs
# ("...running production workloads at scale..."), cited word for word per
# the original-prose and citation rules in ENTRY-TEMPLATE.md.
at scale

# cascading. Plain engineering term for a cascading failure in a
# distributed system (control-plane overload under a cluster-wide incident),
# not startup-jargon slop.
cascading

# structurally. Plain adverb distinguishing a structural constraint from a
# process convention, per the repo's own prior justification for this word.
structurally

# typically. Plain hedge adverb for honest, non-absolute claims per the
# judgement-vs-sourced-claim contract in ENTRY-TEMPLATE.md.
typically
end-to-end
end to end

# decode. Standard, unavoidable technical verb for base64/JWT decoding
# functions and variable names in the code samples (base64.b64decode,
# DecodeString, decode()), not figurative AI-slop usage.
decode

# SagaTestHarness. Real MassTransit API class name, cited verbatim because
# renaming a documented library identifier would misquote the source.
SagaTestHarness
# encompass. Verbatim inside a direct quote from the Azure Architecture
# Center Busy Front End antipattern page ("...the term resource can
# encompass many things..."), cited word for word per the original-prose
# and citation rules in ENTRY-TEMPLATE.md.
encompass

# Apache Ignite. Proper noun, the name of a real Apache Software
# Foundation project, cited as a named production use per ENTRY-TEMPLATE.md.
# Not the verb "ignite" used figuratively.
ignite


# embrace. Verbatim inside the real subtitle of Kent Beck's book, Extreme
# Programming Explained. Embrace Change, cited as a source in multiple
# entries. Renaming the title would misquote the source.
embrace

# basically. Not figurative filler. Literal first word of the pattern's own
# canonical name, the BASE acronym (Basically Available, Soft state,
# Eventually consistent), coined by Dan Pritchett, ACM Queue 2008. The
# pattern cannot be named or explained without this exact term of art.
basically

# robust. Verbatim inside the real title of RFC 9413, "Maintaining Robust
# Protocols" (Thomson and Schinazi, IETF/IAB, 2023), cited as a source in
# this entry. Renaming the title would misquote the source, per the
# original-prose and citation rules in ENTRY-TEMPLATE.md.
robust

# ecosystem. Plain, unavoidable technical term for the idiomatic.md entry,
# denoting a language's community, standard library, and tooling as a
# whole (Go ecosystem, Rust ecosystem, Python ecosystem), the subject
# matter of the "Idiomatic" principle itself. Not figurative startup slop.
ecosystem

# maximize/minimize. Verbatim inside a direct quote from Jimmy Bogard's
# "Vertical Slice Architecture" ("minimize coupling between slices, and
# maximize coupling in a slice"), cited word for word per the original-prose
# and citation rules in ENTRY-TEMPLATE.md.
maximize
minimize

# decoding/decoded. Same standard technical verb as "decode" above, inflected
# forms used for the same base64/JWT/session-token verification functions in
# this entry's code samples and prose. Not figurative AI-slop usage.
decoding
decoded

# reshape/reshapes/reshaping/reshaped. Plain, unavoidable technical verb for
# the Normalizer and Message Translator pattern family, describing the act
# of converting a message from one structural shape to another. Not
# figurative AI-slop usage, this is the literal subject matter of the entry.
reshape
reshapes
reshaping
reshaped

# ceiling. Plain technical term for an upper scaling bound (a format-count
# ceiling, a throughput ceiling), used extensively elsewhere in this repo
# (throttling.md, retry-budget.md, deployment-stamps.md, and more). Not
# figurative AI-slop usage.
ceiling

# unlocking/unlocked. Inflected forms of "unlock", already allowed above.
# Literal technical term for mutex release, the exact subject matter of the
# Scoped Locking entry (lock acquisition and release discipline). Not
# figurative AI-slop usage.
unlocking
unlocked

# Spark/spark. Proper noun, Apache Spark, the Apache Software Foundation
# project cited as a named production use and implementation variant
# throughout patterns/09-concurrency/map-reduce.md, per ENTRY-TEMPLATE.md.
# Not the figurative AI-slop verb "spark" (to spark curiosity).
spark

# Pivotal. Proper noun, Pivotal Software Inc., one of the named companies
# whose engineers formed the Reactive Streams initiative in 2013, cited as
# part of the pattern's real lineage per ENTRY-TEMPLATE.md dimension 1. Not
# the figurative AI-slop adjective "pivotal moment".
pivotal

# paradigms. Verbatim inside the real subtitle of Brendan Burns'
# Designing Distributed Systems ("Patterns and Paradigms for Scalable,
# Reliable Services"), cited as a source in this entry. Renaming the
# title would misquote the source, per the original-prose and citation
# rules in ENTRY-TEMPLATE.md.
paradigms
paradigm

# canvas/canvases. Plain noun, the literal name and subject matter of the
# Context Canvas (Bounded Context Canvas) entry, a named DDD strategic
# design worksheet created by Nick Tune. Not figurative AI-slop usage.
canvas
canvases

# production-ready. Verbatim inside the real title of Uber Engineering's
# "Designing a Production-Ready Kappa Architecture for Timely Data Stream
# Processing" (Uber Blog), cited as a source in patterns/12-data-storage/
# kappa-architecture.md, per ENTRY-TEMPLATE.md. Renaming the title would
# misquote the source.
production-ready

# potential. Verbatim inside a direct quote from the Role Object paper's
# own Applicability section ("your potential roles have strong
# interdependencies"), cited word for word per the original-prose and
# citation rules in ENTRY-TEMPLATE.md.
potential

# to this end. Verbatim inside a direct quote from the Role Object paper's
# own Collaborations section ("...it describes the desired roles with
# specification objects."), cited word for word per the original-prose
# and citation rules in ENTRY-TEMPLATE.md.
to this end
