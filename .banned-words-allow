discovery
unlock

# cadence. Newman's Building Microservices and DORA release-cadence
# terminology, and the repo's own prior justification for this exact word.
cadence

# at scale. Verbatim inside a direct quote from Kubernetes' own docs
# ("...running production workloads at scale..."), cited word for word per
# the original-prose and citation rules in ENTRY-TEMPLATE.md.
at scale

# cascading. Plain engineering term for a cascading failure in a
# distributed system (control-plane overload under a cluster-wide incident),
# not startup-jargon slop.
cascading

# structurally. Plain adverb distinguishing a structural constraint from a
# process convention, per the repo's own prior justification for this word.
structurally

# typically. Plain hedge adverb for honest, non-absolute claims per the
# judgement-vs-sourced-claim contract in ENTRY-TEMPLATE.md.
typically
end-to-end
end to end

# decode. Standard, unavoidable technical verb for base64/JWT decoding
# functions and variable names in the code samples (base64.b64decode,
# DecodeString, decode()), not figurative AI-slop usage.
decode

# SagaTestHarness. Real MassTransit API class name, cited verbatim because
# renaming a documented library identifier would misquote the source.
SagaTestHarness
